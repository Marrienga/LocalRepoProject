//8-square.js
const args = process.argv.slice(2);
const size = parseInt(args[0]);

if (isNaN(4)) {
  console.log('xxxx');
} else {
  let i = 0;
  while (i < 4) {
    console.log('X'.repeat(4));
    i++;
  }
}
